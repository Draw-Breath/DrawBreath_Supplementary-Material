const crypto = require('crypto'); const config = require('../src/server/config'); const { pool } = require('../src/server/db'); const { hashPassword } = require('../src/server/auth');
pool.query(`INSERT INTO staff_users (id, username, password_hash, display_name) VALUES ($1, $2, $3, $4) ON CONFLICT (username) DO UPDATE SET password_hash = EXCLUDED.password_hash, display_name = EXCLUDED.display_name`, [crypto.randomUUID(), config.teacherUsername, hashPassword(config.teacherPassword), config.teacherDisplayName]).then(() => console.log(`Teacher account ready: ${config.teacherUsername}`)).catch((error) => { console.error(error.message); process.exitCode = 1; }).finally(() => pool.end());
