const path = require('path'); const { spawn } = require('child_process'); require('dotenv').config({ path: path.join(__dirname, '..', '.env') }); const root = path.join(__dirname, '..'); const children = [spawn(process.execPath, ['--watch', 'src/server/index.js'], { cwd: root, stdio: 'inherit', windowsHide: true }), spawn(process.execPath, [path.join(root, 'node_modules', 'vite', 'bin', 'vite.js'), '--port', '5300', '--strictPort'], { cwd: root, stdio: 'inherit', windowsHide: true })]; const stop = () => children.forEach((child) => child.kill('SIGTERM')); process.on('SIGINT', stop); process.on('SIGTERM', stop);
